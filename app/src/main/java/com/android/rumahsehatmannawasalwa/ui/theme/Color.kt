package com.android.rumahsehatmannawasalwa.ui.theme

import androidx.compose.ui.graphics.Color

// ── GREEN CORE (Herbal Theme) ─────────────────────────────────────────────────
val GreenDark      = Color(0xFF1B5E20)  // Header gradient gelap
val GreenPrimary   = Color(0xFF2E7D32)  // Primary button, teks hijau utama
val GreenLight     = Color(0xFF4CAF50)  // Highlight, gradient bawah, badge selesai
val GreenSoft      = Color(0xFFE8F5E9)  // Background card, chip, placeholder
val GreenContainer = Color(0xFFDCEDC8)  // Background card aktif/terpilih
val BackgroundLight = Color(0xFFF9F9F9)

// ── TEXT ─────────────────────────────────────────────────────────────────────
val SlateText      = Color(0xFF334155)  // Judul, teks card utama
val SlateTextDark  = Color(0xFF1E293B)  // Teks heading lebih gelap
val TextPrimary    = Color(0xFF1B1B1B)  // Body text biasa
val TextSecondary  = Color(0xFF757575)  // Teks sekunder / placeholder
val GrayText       = Color(0xFF94A3B8)  // Label kecil, ikon, durasi
val BodyGray       = Color(0xFF64748B)  // Deskripsi panjang, body text

// ── BACKGROUND & SURFACE ─────────────────────────────────────────────────────
val BackgroundWhite = Color(0xFFF8F9FA) // Scaffold background
val SurfaceLight    = Color(0xFFF8FAFC) // Card ringan, gradient card
val SurfaceGrey     = Color(0xFFF5F5F5) // Card abu-abu
val OnSurfaceBlack  = Color(0xFF1C1B1F) // Teks hitam M3

// ── DIVIDER & BORDER ─────────────────────────────────────────────────────────
val DividerLight = Color(0xFFF1F5F9)    // Divider halus, border card
val DividerColor = Color(0xFFE0E0E0)    // Divider standar
val SlateDisabled = Color(0xFFBFCABA)   // Border unfocused

// ── STATUS (Booking) ─────────────────────────────────────────────────────────
val StatusPending   = Color(0xFFFFC107) // Menunggu — Amber
val StatusConfirmed = Color(0xFF2196F3) // Dikonfirmasi — Biru
val StatusCompleted = Color(0xFF4CAF50) // Selesai — Hijau
val StatusCancelled = Color(0xFFEF5350) // Dibatalkan — Merah
val StatusWarning   = Color(0xFFFF9800) // Peringatan jadwal — Oranye
val PaymentWarning    = Color(0xFFE65100) // Countdown pembayaran — Oranye tua
val PaymentWarningBackground = Color(0xFFFFF3E0) // Background light untuk warning pembayaran
val AmberSoft  = Color(0xFFFFF8E1) // Background light untuk status pending
val BlueSoft   = Color(0xFFE3F2FD) // Background light untuk status info/proses
val RedSoft    = Color(0xFFFFEBEE) // Background light untuk status bahaya/batal

// ── DANGER & ERROR ────────────────────────────────────────────────────────────
val RedDanger                = Color(0xFFD32F2F) // Tombol hapus/bahaya
val ErrorContainerSnackbar   = Color(0xFFFFDAD6)
val OnErrorContainerSnackbar = Color(0xFF93000A)

// ── SNACKBAR ──────────────────────────────────────────────────────────────────
val PrimaryContainerSnackbar = Color(0xFF0D631B)

// ── ADMIN ACCENT (Stats/Chips) ────────────────────────────────────────────────
val AccentEmerald = Color(0xFF10B981)   // Stat hijau emerald
val AccentBlue    = Color(0xFF3B82F6)   // Stat biru
val AccentAmber   = Color(0xFFF59E0B)   // Stat amber
val AccentPink    = Color(0xFFEC4899)   // Label pink
val AccentOrange  = Color(0xFFF97316)   // Label oranye
val AccentTeal    = Color(0xFF14B8A6)   // Label teal